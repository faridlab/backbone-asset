//! Asset seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial Asset data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// ASSET SEEDER
// ============================================================================

/// Seeder for asset
pub struct SeedAssetSeeder;

impl SeedAssetSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedAssetSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedAssetSeeder {
    fn name(&self) -> &'static str {
        "SeedAssetSeeder"
    }

    fn order(&self) -> i32 {
        2
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if asset.assets table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM asset.assets")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/asset_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM asset.assets")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

